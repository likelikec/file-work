文件介绍：
    api包：调用LLM对数据集进行分类，需要上传每个项目的src目录
    data包：数据预处理，包括数据清洗、打标签、标记后的数据集与指标集进行匹配、数据集的划分
    metric select包：特征分析，包括互信息，单变量逻辑回归、P-P图、Q-Q图以及斯皮尔逊相关性分析
    model包：模型训练，包括BP神经网络、调用BP模型进行分类、决策树模型、机器学习四个模型、随机森林模型


流程：
    分别使用evosuite和testart生成测试用例，并统计覆盖率，得到文件1和2——>使用data包下的excel-label进行合并并打标签——>使用idea插件计算各种指标后得到文件3
 ————>使用data包下excel-match，将指标集与标签集匹配————>使用data包下的fenlei进行数据清洗和划分————>使用model包下的各种模型进行训练